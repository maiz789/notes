; Calculate fibonacci number at an index "n"
; n=0, Fib(n)=0
; n=1, Fib(n)=1
; n=any, Fib(n)=Fib(n-1) + Fib(n-2)

; Lib
; (define (<name> <input>) (operation on input))
; (if (condition? todo if cond is true))

(define (fib n) ())